var roleHarvester = {

    /** @param {Creep} creep **/
    run: function(creep) {
        var storage = creep.room.storage;
        var terminal = creep.room.terminal;
        
        
        
	}
};

module.exports = roleHarvester;